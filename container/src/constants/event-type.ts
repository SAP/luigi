export interface ParamsEvent extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent2 extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent3 extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent4 extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent5 extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent6 extends Event {
    // isAcceptable(s: string): boolean;
}

export interface ParamsEvent7 extends Event {
    // isAcceptable(s: string): boolean;
}
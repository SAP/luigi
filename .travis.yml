language: node_js
node_js:
  - 'node'
install: npm install --prefix core
jobs:
  include:
    - stage: 'Tests' # naming the Tests stage
      name: 'Unit Tests' # names the first Tests stage job
      script: npm test --prefix core
    - script: ./test/e2e.sh #node ./test/e2e.js
      name: 'Integration Tests' # names the second Tests stage job
#    - stage: deploy
#      name: "Deploy to GCP"
#      script: ./deploy
